codecov:
  require_ci_to_pass: yes

comment:
  layout: "header, coverage, diff, flags, files"
  require_changes: false
  require_base: false
  after_n_builds: 1

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        base: auto
    patch:
      default:
        target: auto
        threshold: 0%
        base: auto

flag_management:
  default_rules:
    carryforward: true
